**技术栈选择：**
- 语言：Python 3.8+
- 无第三方库依赖

**架构分层：**
- 输入层：`input()` 获取用户输入
- 业务逻辑层：`add()`, `subtract()`, `multiply()`, `divide()` 四个纯函数
- 调度层：`operations` 字典映射运算符到对应函数
- 异常处理：除零错误捕获

**关键设计决策：**
1. 使用字典映射替代 if-elif 链，提高可扩展性
2. 四个运算函数保持纯函数风格，不处理I/O
3. 异常处理集中在调度层，避免函数内部混杂错误处理

STAGE_DONE: architecture