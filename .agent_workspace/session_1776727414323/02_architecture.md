**技术栈选择：**
- 语言：Python 3.x
- 框架：无（标准库）
- 第三方库：无

**架构设计：**
- 单文件脚本，采用面向过程或简单函数式结构。
- 核心模块：`main` 函数控制流程，`calculate` 函数执行运算逻辑，`get_user_input` 函数处理输入和验证。
- 错误处理：使用 `try...except` 捕获除零错误和类型转换错误。

**关键设计决策：**
1.  **交互方式**：基于命令行的交互式循环，直到用户选择退出。
2.  **运算支持**：支持加 (+)、减 (-)、乘 (*)、除 (/) 四种基本运算。
3.  **输入验证**：对用户输入的数字进行 `float` 转换验证，对运算符进行有效性检查。
4.  **错误处理**：对除零和无效输入提供友好的错误提示。

STAGE_DONE: architecture