**技术栈选择：**
- **编程语言**：Python 3
- **依赖**：无（仅使用标准库）

**架构设计：**
这是一个单文件控制台应用程序，无需分层。代码将直接包含一个主入口点，执行打印“Hello World”的操作。

**代码规范：**
- 使用 `main` 函数作为程序入口。
- 使用 `if __name__ == "__main__":` 来调用 `main` 函数。
- 遵循 PEP 8 代码风格。

STAGE_DONE: architecture