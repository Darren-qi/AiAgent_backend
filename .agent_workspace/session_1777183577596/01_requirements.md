好的，需求已明确。以下是基于你描述的需求文档：

### 功能清单

1.  **项目分层结构**：建立清晰的分层架构，如接口层、业务逻辑层、数据访问层，以支持项目的可维护性和扩展性。
2.  **MySQL数据库工具**：封装一个通用的MySQL数据库操作类，支持基本的增删改查操作。
3.  **Doris数据库工具**：封装一个通用的Doris数据库操作类，支持基本的增删改查操作。
4.  **统一配置管理**：支持通过配置文件管理数据库连接等环境变量。

### 验收标准

-   用户可以通过配置文件轻松切换和配置MySQL与Doris的连接信息。
-   用户可以在业务代码中直接调用封装好的MySQL工具类，完成数据的增、删、改、查操作，无需编写原生SQL连接代码。
-   用户可以在业务代码中直接调用封装好的Doris工具类，完成数据的增、删、改、查操作。
-   项目启动后，分层结构清晰，开发者可以快速定位到接口层、业务逻辑层和数据访问层的对应代码位置。

STAGE_DONE: requirements